model: # model hyper-parameters
  architecture: Multi_FSB_BTS_UNet
  sequences: 1
  width: 24
  deep_supervision: True

optimizer:
  opt: Adam
  lr: 0.001

loss:
  function:  DICE

training:
  debug: False
  seed: 1993
  epochs: 5000
  max_patience: 100
  CV: 4
  cuda_benchmark: False

data:
  input_img: Datasets/Dataset_BUSI_with_GT_postprocessed_128
  batch_size: 1
  train_size: 0.8
  classes: [malignant]
  remove_outliers: False
  oversampling: False
  augmentation:
    CLAHE: False
    brightness_brighter: False
    brightness_darker: False
    contrast_high: False
    contrast_low: False
  transforms:
    horizontal_flip: .5
    vertical_flip: .5
    rotation: 1.0